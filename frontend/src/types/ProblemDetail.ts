/*
 * A mirror of the ProblemDetail class in Spring Boot.
 * Made this just to be able to print Java exceptions in the frontend console.
 */

export interface ProblemDetail {
    title: string
    detail: string
}